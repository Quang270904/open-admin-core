name: unittest

on:
  pull_request:
    paths:
      - 'src/**'
      - 'tests/**'
      - 'resources/**'
      - 'database/**'
      - 'config/**'

jobs:
  unittest:
    runs-on: ubuntu-latest
    services:
      db:
        image: mysql:5.7
        ports:
          - 3306:3306
        env:
          MYSQL_ROOT_PASSWORD: password
          MYSQL_DATABASE: laravel_admin_test
          MYSQL_USER: mysql
          MYSQL_PASSWORD: password
        options: >-
          --health-cmd "mysqladmin ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    strategy:
      fail-fast: false
      matrix:
        php: [ "8.2", "8.3" ]
    steps:
      - uses: actions/checkout@v3

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          extensions: mbstring, xml, ctype, json, mysql, dom, pcre, gd
          coverage: none
          ini-values: memory_limit=512M

      - name: Composer install
        run: composer install --no-interaction --prefer-dist --optimize-autoloader

      - name: Run PHPUnit tests
        run: php -d memory_limit=512M ./vendor/bin/phpunit --display-warnings --display-notices
        env:
          MYSQL_HOST: 127.0.0.1
